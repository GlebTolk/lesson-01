// С помощью цикла while найдите сумму чисел от 1 до 20
// Результат присвойте переменной sum

// your code
// your code
let sum = function () {
    // Инициализируем начальное значение суммы и счетчик как свойства самого объекта sum
    sum.total = (sum.total || 0) + (sum.i || 1);
    sum.i = (sum.i || 1) + 1;
    
    // Условие продолжения цикла: если счетчик меньше или равен 20,
    // повторно вызываем функцию sum() для следующей итерации
    if ((sum.i - 1) <= 20) {
        sum();
    }
};

// Запускаем вычисление
sum();

// Присваиваем итоговое число из свойств функции обратно в примитивную переменную sum
sum = sum.total;